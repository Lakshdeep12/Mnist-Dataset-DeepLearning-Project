# Mnist-Dataset-DeepLearning-Project
A deep learning project for handwritten digit classification using the MNIST dataset. The project demonstrates how a neural network can learn to recognize handwritten digits (0–9) from grayscale images.
